import css from './LocationInfo.module.css';

const LocationInfo = () => {
    return (
        <div className={css.LocationInfo}>
            LocationInfo
        </div>
    );
};

export {
    LocationInfo
};
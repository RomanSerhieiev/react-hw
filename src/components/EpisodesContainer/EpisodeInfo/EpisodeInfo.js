import css from './EpisodeInfo.module.css';

const EpisodeInfo = () => {
    return (
        <div className={css.EpisodeInfo}>
            EpisodeInfo
        </div>
    );
};

export {
    EpisodeInfo
};